'use client';
import {useEffect,useState} from 'react';
export default function ProjectGallery({images,title}:{images:{image_url:string;alt_text:string}[],title:string}){const [i,setI]=useState(0);useEffect(()=>{if(images.length<2)return;const t=setInterval(()=>setI(x=>(x+1)%images.length),3000);return()=>clearInterval(t)},[images.length]);if(!images.length)return <div className="preview">PROJECT PREVIEW</div>;return <div className="gallery"><img src={images[i].image_url} alt={images[i].alt_text||title}/>{images.length>1&&<><button className="prev" onClick={()=>setI(x=>(x-1+images.length)%images.length)}>‹</button><button className="next" onClick={()=>setI(x=>(x+1)%images.length)}>›</button><div className="dots">{images.map((_,n)=><button key={n} className={n===i?'active':''} onClick={()=>setI(n)} aria-label={"Image "+(n+1)}/>)}</div></>}</div>}