import {notFound} from 'next/navigation';import {supabase} from '@/lib/supabase';import ProjectGallery from '@/components/ProjectGallery';
export const dynamic='force-dynamic';
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const {data:p}=await supabase.from('projects').select('*,project_images(*)').eq('slug',slug).eq('published',true).single();if(!p)notFound();const imgs=(p.project_images||[]).sort((a:any,b:any)=>a.sort_order-b.sort_order);return <main className="listing projectDetail"><ProjectGallery title={p.title} images={imgs}/><p className="eyebrow">{p.project_type||'PROJECT'}</p><h1>{p.title}</h1><p>{p.description}</p><div className="detailGrid"><div><h2>Key Features</h2><ul>{(p.features||[]).map((x:string)=><li key={x}>{x}</li>)}</ul></div><div><h2>Project Details</h2><p><b>Status:</b> {p.status}</p>{p.year&&<p><b>Year:</b> {p.year}</p>}<p><b>My Role:</b> {p.role}</p>{p.website_url&&<a className="button" href={p.website_url} target="_blank">Visit Website →</a>}</div></div></main>}